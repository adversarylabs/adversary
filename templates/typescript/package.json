{
  "name": "{{name}}",
  "version": "{{version}}",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@adversarylabs/sdk": "^0.1.16"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
